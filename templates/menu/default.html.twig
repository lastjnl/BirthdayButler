<div class="mb-2">
    <h1 class="text-2xl">🍹BirthdayButler</h1>

    {% if (app.user) %}
    <div class="bg-red-300 flex flex-col rounded-md p-2">
        <span>Welkom <span class="font-bold">{{app.user.name}}</span></span>
        <a href="<{{ path('app_logout') }}">Uitloggen</a>
    </div>
    {% endif %}


</div>